# 建材直供/出库量 每日增量更新 —— 换机复刻指南

> 换一台新电脑，照着本文档 15 分钟搭好。只做一件事：把 MySteel 建筑钢材出库量 Excel 自动追加到 `钢材直供量统计.xlsx`。

---

## 一、能干什么

拖入一份 MySteel 源 Excel → 一条命令跑完：

- **A-M 列**：日期 + 直供4区 + 线盘4区 + 螺纹4区（12 个数值，自动去重追加）
- **N-AH 列**：21 个公式自动生成（MA5 五日均线、贸易商拿货量、直供占比、螺纹-直供差值）
- **写入前自动备份 `.bak`**，支持 `--dry-run` 只读预览
- **幂等**：重复跑不会堆积，二次跑显示"追加 0 行"

---

## 二、搭环境

```bash
# 1）确认 Python ≥ 3.10
python --version

# 2）装唯一依赖
pip install openpyxl

# 3）拷贝脚本到本地
# 把本文档同目录的 update_zhigong.py 放到你喜欢的位置即可
```

---

## 三、改路径（只改一行）

打开 `update_zhigong.py`，找到带 `⚠️ 换机第一件事` 注释的那行：

```python
# 原行（约第 30 行）
DEFAULT_TGT_PATH = Path(
    r"C:\Users\Administrator\Nutstore\1\小目标\钢材直供量统计.xlsx"
)
```

把 `Administrator` 改成你当前 Windows 用户名，保存。

---

## 四、跑

```bash
# 每次先 dry-run 看一眼（只读不写）
python update_zhigong.py "D:/path/建筑钢材出库量.xlsx" --dry-run

# 确认无误后正式写入
python update_zhigong.py "D:/path/建筑钢材出库量.xlsx"
```

**可选参数**（一般用不到）：
- `--tgt "其他路径.xlsx"` 自定义目标文件
- `--sheet "其他sheet名"` 自定义目标 sheet

---

## 五、看懂输出

```
目标表最后已有日期: 2026-08-03
已备份: ...钢材直供量统计.xlsx.bak
本次追加 5 行：
  row 2223: 2026-08-04
  ...
完成。
```

| 输出 | 含义 |
|---|---|
| `最后已有日期: ...` | 从这天之后追加，已存在的日期全部跳过 |
| `已备份: ...bak` | 自动备份，出问题改后缀即可恢复 |
| `追加 N 行` | 跳过周末/节假日源表无数据日，不补 0 |
| `追加 0 行` | **正常**——目标表已是最新，幂等安全 |

---

## 六、常见问题

### Q1：提示 `'python' 不是内部或外部命令`

Python 没装或没加入 PATH。去 [python.org](https://python.org) 下载安装，安装时勾选「Add Python to PATH」；或用 `py` 命令代替 `python`。

### Q2：`ModuleNotFoundError: No module named 'openpyxl'`

```bash
pip install openpyxl
```

### Q3：目标 Excel 打开了（被锁定）→ 脚本报错

先关闭 Excel 中的目标文件，再跑脚本。

### Q4：源 Excel 格式不对（日期/列数错误）

脚本自动兼容以下情况，一般不用手动处理：

| 可能情况 | 脚本处理 |
|---|---|
| 日期格式 `2026/08/10` 或 `8-10` 或 `8.10` | 自动识别三种分隔符 |
| 数据倒序排列（最新在上） | 内部自动排序升序后再追加 |
| Sheet 名不是"1.1…" | 自动找第一个非"目录"的 sheet |
| 表尾有"返回目录37"等文字 | 日期解析失败自动跳过 |

> 如果源表列数不是 13 列（A=日期 + B-M=数据），脚本会报错。确保是 MySteel 标准导出格式。

### Q5：公式打开显示 `#N/A`

历史前 4 行（2018-01 开头那几行）MA5 不足 5 天数据会 `#N/A`，是正常的。新日期前面有几千行数据，不会出现。

### Q6：换机后路径还写着 `Administrator`

**必须手动改**——回到第三章那一行。

### Q7：想把备份恢复回去

```bash
copy "钢材直供量统计.xlsx.bak" "钢材直供量统计.xlsx"
```

## 附：公式对照（N-AH，脚本自动生成，仅供审阅）

| 列 | 含义 | 公式 |
|---|---|---|
| N-Y | MA5（12 个指标） | `=AVERAGE({col}{r-4}:{col}{r})` |
| Z-AC | 贸易商拿货量 MA5 | `=线盘MA5 + 螺纹MA5 - 直供MA5` |
| AD-AG | 建材直供占比 | `=直供MA5 / (线盘MA5 + 螺纹MA5)` |
| AH | 螺纹出库 - 直供量 | `=J{r} - B{r}` |
