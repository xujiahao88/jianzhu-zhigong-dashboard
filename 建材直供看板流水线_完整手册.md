# 建材直供看板流水线 —— 完整手册 & 换机复刻指南

> 一句话：**丢一份《建筑钢材出库量》Excel 进来，一条命令跑完「更新数据 → 重画看板 → 截长图 → 发微信」，全自动。**
>
> 已稳定跑 6 波（08-13 / 08-14 / 08-20 / 08-21 / 08-27），零故障。

---

## 一、整套流程长什么样

```
┌─────────────────────────────────┐
│  源 Excel（MySteel 出库量日报）   │  你丢进来
└──────────────┬──────────────────┘
               ▼
  ① update_zhigong.py    追加新日期到目标表 + 生成 21 列公式（自动备份）
               ▼
  ② build_dashboard.py   matplotlib 重画 16 张跟踪图 + 4 张占比图 + 数据表 → index.html
               ▼
  ③ html_to_image.py     Edge headless 截整页 → 裁空白 → 看板长图.png（2480×4028）
               ▼
  ④ send_to_wechat.py    图片入剪贴板 → 激活微信 → Ctrl+V → Enter
               ▼
┌─────────────────────────────────┐
│  微信「文件传输助手」收到长图      │
└─────────────────────────────────┘
```

四步由 `run_pipeline.py` 一个脚本串起来，**任何一步失败立即停，不会带着脏数据往下跑**。

---

## 二、日常怎么用（本机已配好）

### 2.1 最短路径

```bash
cd "C:/Users/Administrator/WorkBuddy/2026-08-11-23-07-07"
"C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe" run_pipeline.py "<源Excel绝对路径>"
```

**发微信前务必先做三件事**：① 微信已登录 ② 手动点开「文件传输助手」会话 ③ 微信窗口在前台。
脚本有 **5 秒倒计时**给你切窗口。

> 更省事的做法：直接把 Excel 拖进 WorkBuddy 说"更新数据"，我会自动跑全流程。

### 2.2 常用参数

| 命令 | 用途 |
|---|---|
| `run_pipeline.py <源路径>` | 完整流程（含发微信） |
| `run_pipeline.py <源路径> --dry-run` | 只更新 + 出图，**不发微信**（想先看图时用） |
| `run_pipeline.py <源路径> --years 6` | 图表保留近 6 年（默认 5 年） |
| `run_pipeline.py <源路径> --recent-days 15` | 底部数据表显示近 15 天（默认 10 天） |

### 2.3 单独跑某一步

```bash
PY="C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe"

# 只更新数据（先看一眼会追加什么）
$PY update_zhigong.py "<源路径>" --dry-run

# 只重画看板（数据没变，想重出图）
$PY build_dashboard.py

# 只重新截图
$PY html_to_image.py

# 只重新发微信（图已经生成好了，想再发一次）
$PY send_to_wechat.py
```

---

## 三、看懂运行输出

一次完整运行的输出长这样（关键行已标注）：

```
目标表最后已有日期: 2026-08-21          ← ① 从这天之后追加
已备份: ...钢材直供量统计.xlsx.bak       ← ② 自动备份，出问题直接改后缀恢复
本次追加 4 行：                          ← ③ 实际写入行数
  row 2237: 2026-08-24
  row 2238: 2026-08-25
  row 2239: 2026-08-26
  row 2240: 2026-08-27
完成。
📖 读取: ...钢材直供量统计.xlsx
   共 2146 行, 日期范围 2018-01-02 ~ 2026-08-27
🔧 计算衍生指标...
📅 筛选近 5 年...
   剩 1195 行 (2022-01-06 ~ 2026-08-27)   ← ④ 看板只画近 5 年
🎨 绘制图表...
   ✅ 建材直供&出库日度跟踪: page1_直供出库.png
   ✅ 建材直供占比 MA5 日度跟踪: page2_直供占比.png
🌐 生成 HTML...  ✅ index.html
✅ 长图已生成: ...dashboard\看板长图.png (2480x4028)   ← ⑤ 长图尺寸
⏰ 5 秒后开始...                          ← ⑥ 切微信窗口的窗口期
📋 图片已放入剪贴板 (2480x4028)
🪟 已激活微信窗口 ((L1388, T486, R3072, B1894))   ← ⑦ 看坐标是否在屏幕内
📎 Ctrl+V 粘贴图片...
📤 Enter 发送...
✅ 发送完成
```

### 几个容易误判的点

| 现象 | 是不是出问题了？ |
|---|---|
| **追加 0 行** | 正常 —— 目标表已是最新，幂等，重复跑不会堆积 |
| **日期跳过几天**（如 08-21 → 08-24） | 正常 —— 22/23 是周末，源表本身停更，**不补 0** |
| 微信窗口坐标 `L` 是负数 | ⚠️ 窗口左侧跑到屏幕外了，粘贴可能落空，去微信确认 |
| `Workbook contains no default style` 警告 | 无害 —— openpyxl 对无默认样式工作簿的常规提示 |
| 历史前几行 MA5 显示 `#N/A` | 正常 —— 2018 年开头数据不足 5 天 |

---

## 四、换机复刻（新电脑 15 分钟搭好）

### 4.1 装依赖

```bash
pip install openpyxl pandas matplotlib numpy Pillow pywinauto pyautogui pywin32
```

Python ≥ 3.10 即可。

### 4.2 拷脚本

把 5 个脚本放**同一个目录**（它们靠 `Path(__file__).parent` 互相定位）：

```
update_zhigong.py      数据更新
build_dashboard.py     看板/图表生成
html_to_image.py       网页转长图
send_to_wechat.py      发微信
run_pipeline.py        串起上面四步
```

> 一份现成副本在 skill 目录：`C:\Users\Administrator\.workbuddy\skills\jianzhu-zhigong-pipeline\scripts\`

### 4.3 改路径（**这步不做一定跑不起来**）

| 文件 | 行 | 常量 | 改成什么 |
|---|---|---|---|
| `update_zhigong.py` | 31-33 | `DEFAULT_TGT_PATH` | 目标 Excel 真实路径 |
| `build_dashboard.py` | 26 | `DEFAULT_EXCEL` | **同上**（两处必须一起改） |
| `run_pipeline.py` | 19 | `PYTHON` | 本机 Python 解释器绝对路径 |
| `html_to_image.py` | 18 | `EDGE` | Edge 安装位置，默认 `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe` |

```python
# update_zhigong.py 第 31-33 行，原样
DEFAULT_TGT_PATH = Path(
    r"C:\Users\Administrator\Nutstore\1\小目标\钢材直供量统计.xlsx"
)
#                  ↑ 把 Administrator 换成你自己的 Windows 用户名
```

### 4.4 验证

```bash
# 1) 先用 dry-run 试，只看不写
python update_zhigong.py "<源路径>" --dry-run

# 2) 确认追加的日期对，再正式跑
python run_pipeline.py "<源路径>" --dry-run

# 3) 图没问题了，去掉 --dry-run 正式跑（含发微信）
python run_pipeline.py "<源路径>"
```

---

## 五、目标表结构（`钢材直供量统计.xlsx` → `源数据-更新`）

一共 34 列 = 1 列日期 + 12 列数据 + 21 列公式。

| 列 | 含义 | 公式 |
|---|---|---|
| A | 日期 | 来自源表 |
| B-E | 建材直供量：全国 / 华东 / 南方 / 北方 | 来自源表 |
| F-I | 线盘出库量：全国 / 华东 / 南方 / 北方 | 来自源表 |
| J-M | 螺纹出库量：全国 / 华东 / 南方 / 北方 | 来自源表 |
| N-Y | 上述 12 个指标的 MA5 | `=AVERAGE({col}{r-4}:{col}{r})` |
| Z-AC | 贸易商拿货量 MA5 | `=线盘MA5 + 螺纹MA5 - 直供MA5` |
| AD-AG | 建材直供占比 | `=直供MA5 / (线盘MA5 + 螺纹MA5)` |
| AH | 螺纹出库 - 直供量 | `=J{r} - B{r}` |

> 公式全部由脚本自动生成，你不需要手写。写前 4 行历史数据 MA5 不足会 `#N/A`，属正常。

---

## 六、踩坑清单（都已修好，**别改回去**）

| # | 坑 | 当时现象 | 修法 |
|---|---|---|---|
| 1 | **子进程 stdout 回退 GBK** | 打印 emoji（📖）时 `UnicodeEncodeError`，整条流水线崩在出图步 | `run_pipeline.py` 里 `env = os.environ.copy()` 继承系统环境，再强制 `PYTHONIOENCODING=utf-8` |
| 2 | **图表 region 靠字符串猜** | 前 3 行标题全变 `建材直供量(MA5)-MA5`，4 张图长得一模一样 | 别用 `col.split("_")[-1]`（对 `建材直供量_全国_MA5` 会拿到 `MA5`）；直接按列索引取 `regions[j]`，列顺序固定是 [全国, 华东, 南方, 北方] |
| 3 | **Edge `--screenshot` 用相对路径** | 截图跑到 Edge 自己的 cwd，截出空白图 | 必须传绝对路径 |
| 4 | **微信 4.0 是 Qt 自绘** | pywinauto 只能看到 2 个空 Pane，控件树完全不可见，没法点输入框 | 不走控件定位，改「剪贴板 + 键盘快捷键」 |
| 5 | 源表日期倒序、表尾有"返回目录37" | 追加乱序或写入垃圾行 | 内部排序 + 日期解析失败自动跳过 |
| 6 | 目标 Excel 被打开占用 | openpyxl 写失败 | 跑脚本前关闭 Excel |

---

## 七、常见问题

**Q1：提示 `'python' 不是内部或外部命令**
Python 没加 PATH。用绝对路径（如手册里的 `C:/Users/.../python.exe`），或重装时勾「Add Python to PATH」。

**Q2：`ModuleNotFoundError: No module named 'xxx'`**
`pip install xxx`，依赖清单见 4.1。

**Q3：微信没收到图 / 发到别的会话了**
绝大多数是**微信当时不在「文件传输助手」会话**。手动点开会话，然后单独重发：`python send_to_wechat.py`。
如果日志里窗口坐标 `L` 是负数，先把微信窗口拖回屏幕中间再发。

**Q4：源表列数不是 13 列怎么办**
脚本要求 MySteel 标准导出格式（A=日期 + B-M=12 个数值）。格式变了要改 `update_zhigong.py` 里的 `COL_NAMES`。

**Q5：想把备份恢复回去**
```bash
copy "钢材直供量统计.xlsx.bak" "钢材直供量统计.xlsx"
```

**Q6：只想改图的配色 / 年份 / 线条**
都在 `build_dashboard.py` 里：调色板、虚实线、`--years` 参数。当前配色是——当年红实线 `#E41A1C`、上年深蓝实线 `#08306B`、2024 深蓝虚线 `#08519C`、2023 亮蓝虚线 `#3182BD`、2022 灰色虚线 `#A6A6A6`。

**Q7：长图底部留白太多 / 被裁多了**
`html_to_image.py` 里 `crop_bottom = last_content + 30`，改那个 `30` 调余量。

---

## 八、文件清单

| 文件 | 作用 |
|---|---|
| `run_pipeline.py` | 总入口，串起 4 步 |
| `update_zhigong.py` | ① 数据更新（幂等追加 + 公式生成 + 自动备份） |
| `build_dashboard.py` | ② 算 MA5/占比，出 20 张图 + 数据表 + index.html |
| `html_to_image.py` | ③ Edge headless 截图 + 裁剪 |
| `send_to_wechat.py` | ④ 剪贴板 + 键盘模拟发送 |
| `dashboard/index.html` | 网页看板（可直接浏览器打开） |
| `dashboard/page1_直供出库.png` | 16 张跟踪图 |
| `dashboard/page2_直供占比.png` | 4 张占比图 |
| `dashboard/看板长图.png` | **最终产物**，发微信的就是它 |

**关键路径备忘**
- 工作区：`C:\Users\Administrator\WorkBuddy\2026-08-11-23-07-07\`
- 目标 Excel：`C:\Users\Administrator\Nutstore\1\小目标\钢材直供量统计.xlsx`
- Skill 副本：`C:\Users\Administrator\.workbuddy\skills\jianzhu-zhigong-pipeline\`
- Python：`C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe`

---

*内容由 AI 基于本地脚本与实际运行记录整理，仅供内部使用。*
